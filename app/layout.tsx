import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Hospitality Simplified | Voyaaz",
  description: "Discover how Voyaaz disrupted the hotel industry by creating a new category, leveraging modern tech like Ruby on Rails, and prioritizing seamless, mobile-first guest experiences.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
